#include <stdio.h>

void first(void) __attribute__ ((constructor));

/**
 * first - prints a sentence
 */
void first(void)
{
	printf("Mr.tortoise,\n");
	printf(a tortoise having pretty good sende of a hare, challenges one to a race\n");
}
