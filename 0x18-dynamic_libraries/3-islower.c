#include "main.h"

/**
 * _islower - This code checks for lower case character
 * @c: This is th character to be checked
 * Return: 1 for lower case or 0 for anything else
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
